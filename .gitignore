# ──────────────────────────────────────────────────────────────
# Stitchy London — PUBLIC repo (GitHub + jsDelivr CDN)
#
# Only the custom code layer (assets/) and the Webflow Custom Code
# snippets are committed — Webflow pulls them via jsDelivr @main.
# Internal build notes and local tooling are kept out.
# ──────────────────────────────────────────────────────────────

# OS / editor cruft
.DS_Store
.vscode/

# Local Claude Code config
.claude/

# CSS converter scratch output (build-global-css.js → build/global.css)
build/

# Internal build notes (local only, not for public repo)
CLAUDE.md
PROJECT_PROGRESS.md
handover/
WEBFLOW_PLAN.md
CMS_COLLECTIONS.md
ASSETS.md
CSS_LAYER_MAP.md
css-code-struture.md
memory/
